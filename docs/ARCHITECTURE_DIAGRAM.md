# 📐 LearnTube Architecture Diagram

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         MOBILE APP (React Native)                │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │                 PRESENTATION LAYER                      │    │
│  │                  (/app - Screens)                       │    │
│  ├────────────────────────────────────────────────────────┤    │
│  │  • index.tsx (Landing)                                  │    │
│  │  • (auth)/ - Sign in/Sign up                           │    │
│  │  • onboarding.tsx (Initial setup)                      │    │
│  │  • (tabs)/ - Main app screens:                         │    │
│  │    ├─ home.tsx (Video feed)                            │    │
│  │    ├─ roadmap.tsx (Learning path)                      │    │
│  │    ├─ courses.tsx (Saved playlists)                    │    │
│  │    └─ discussion.tsx (AI chat)                         │    │
│  │  • profile.tsx (User settings)                         │    │
│  └────────────────────────────────────────────────────────┘    │
│                            ↕                                     │
│  ┌────────────────────────────────────────────────────────┐    │
│  │                  COMPONENT LAYER                        │    │
│  │                  (/components)                          │    │
│  ├────────────────────────────────────────────────────────┤    │
│  │  • VideoCard - Video display                           │    │
│  │  • PlaylistPlayer - Full player                        │    │
│  │  • AIChatModal - Chat interface                        │    │
│  │  • FormField - Input fields                            │    │
│  │  • ErrorBoundary - Error catching                      │    │
│  └────────────────────────────────────────────────────────┘    │
│                            ↕                                     │
│  ┌────────────────────────────────────────────────────────┐    │
│  │                   STATE LAYER                           │    │
│  │                  (/context)                             │    │
│  ├────────────────────────────────────────────────────────┤    │
│  │  • GlobalProvider                                       │    │
│  │    ├─ User authentication state                        │    │
│  │    ├─ Current user data                                │    │
│  │    └─ Login/logout functions                           │    │
│  └────────────────────────────────────────────────────────┘    │
│                            ↕                                     │
│  ┌────────────────────────────────────────────────────────┐    │
│  │                  BUSINESS LOGIC LAYER                   │    │
│  │                  (/lib & /utils)                        │    │
│  ├────────────────────────────────────────────────────────┤    │
│  │  • appwrite.ts - Backend API calls                     │    │
│  │  • youtube.ts - Video fetching                         │    │
│  │  • gemini.ts - AI roadmap generation                   │    │
│  │  • perplexity.ts - AI chat                             │    │
│  │  • errorHandler.ts - Error management                  │    │
│  └────────────────────────────────────────────────────────┘    │
│                            ↕                                     │
└─────────────────────────────────────────────────────────────────┘
                             ↕
        ┌────────────────────┴────────────────────┐
        ↓                    ↓                     ↓
┌──────────────────┐  ┌──────────────┐  ┌─────────────────┐
│   APPWRITE       │  │  YOUTUBE API │  │  AI SERVICES    │
│   (Backend)      │  │              │  │                 │
├──────────────────┤  ├──────────────┤  ├─────────────────┤
│ • Authentication │  │ • Video      │  │ • Gemini AI     │
│ • Database       │  │   Search     │  │   (Roadmaps)    │
│ • User Data      │  │ • Playlists  │  │ • Perplexity    │
│ • Preferences    │  │ • Metadata   │  │   (Chat)        │
│ • Roadmaps       │  │              │  │                 │
└──────────────────┘  └──────────────┘  └─────────────────┘
```

---

## Data Flow Diagrams

### 1. User Registration & Onboarding Flow

```
User Opens App
      ↓
┌─────────────┐
│  index.tsx  │ ← Check: Is user logged in?
└─────────────┘
      ↓ NO
┌─────────────┐
│ sign-up.tsx │ ← User enters email/password
└─────────────┘
      ↓
┌─────────────────────┐
│  appwrite.ts        │ ← Call createUser()
│  (createUser)       │
└─────────────────────┘
      ↓
┌─────────────────────┐
│  Appwrite Server    │ ← Create account
└─────────────────────┘
      ↓ SUCCESS
┌─────────────────────┐
│  onboarding.tsx     │ ← Select categories
└─────────────────────┘
      ↓
┌─────────────────────┐
│  appwrite.ts        │ ← Save preferences
│  (createDocument)   │
└─────────────────────┘
      ↓
┌─────────────────────┐
│  home.tsx           │ ← Main app
│  (tabs)             │
└─────────────────────┘
```

### 2. Personalized Feed Generation

```
User on Home Screen
      ↓
┌─────────────────────┐
│  home.tsx           │ ← Load user preferences
└─────────────────────┘
      ↓
┌─────────────────────┐
│  appwrite.ts        │ ← Fetch user categories
│  (getAllDocuments)  │
└─────────────────────┘
      ↓
┌─────────────────────┐
│  Appwrite DB        │ → ["Python", "Web Dev"]
└─────────────────────┘
      ↓
┌─────────────────────┐
│  youtube.ts         │ ← Search for videos
│  (searchVideos)     │   using categories
└─────────────────────┘
      ↓
┌─────────────────────┐
│  YouTube API        │ → Returns video list
└─────────────────────┘
      ↓
┌─────────────────────┐
│  Cache (24hr)       │ ← Store results
└─────────────────────┘
      ↓
┌─────────────────────┐
│  VideoCard          │ ← Display videos
│  (Component)        │   in feed
└─────────────────────┘
      ↓
User sees personalized videos
```

### 3. AI Roadmap Generation

```
User Opens Roadmap Tab
      ↓
┌─────────────────────┐
│  roadmap.tsx        │ ← Check if roadmap exists
└─────────────────────┘
      ↓
┌─────────────────────┐
│  appwrite.ts        │ ← Query roadmaps collection
└─────────────────────┘
      ↓ NOT FOUND
┌─────────────────────┐
│  Get user           │ ← Fetch user categories
│  categories         │   from database
└─────────────────────┘
      ↓
┌─────────────────────┐
│  gemini.ts          │ ← Send categories to AI
│  (generate)         │   "Python, Web Dev"
└─────────────────────┘
      ↓
┌─────────────────────┐
│  Gemini AI API      │ ← Process request
└─────────────────────┘
      ↓
┌─────────────────────┐
│  AI Response        │ → Structured roadmap
│                     │   {
│                     │     steps: [
│                     │       "Learn basics",
│                     │       "Build projects",
│                     │       ...
│                     │     ]
│                     │   }
└─────────────────────┘
      ↓
┌─────────────────────┐
│  appwrite.ts        │ ← Save roadmap
│  (createDocument)   │
└─────────────────────┘
      ↓
┌─────────────────────┐
│  roadmap.tsx        │ ← Display roadmap
│                     │   with steps
└─────────────────────┘
      ↓
User sees personalized learning path
```

### 4. AI Chat Interaction

```
User Opens Discussion Tab
      ↓
┌─────────────────────┐
│  discussion.tsx     │ ← Load previous messages
└─────────────────────┘
      ↓
┌─────────────────────┐
│  User types         │ ← "What is React?"
│  message            │
└─────────────────────┘
      ↓
┌─────────────────────┐
│  Get user context   │ ← User's categories:
│                     │   ["Web Dev", "JavaScript"]
└─────────────────────┘
      ↓
┌─────────────────────┐
│  perplexity.ts      │ ← Send message + context
│  (chat)             │
└─────────────────────┘
      ↓
┌─────────────────────┐
│  Perplexity AI      │ ← Generate response
│                     │   considering learning
│                     │   context
└─────────────────────┘
      ↓
┌─────────────────────┐
│  AI Response        │ → "React is a JavaScript
│                     │    library for building
│                     │    user interfaces..."
└─────────────────────┘
      ↓
┌─────────────────────┐
│  AIChatModal        │ ← Display response
│  (Component)        │   in chat UI
└─────────────────────┘
      ↓
┌─────────────────────┐
│  appwrite.ts        │ ← Save conversation
│  (createDocument)   │   to database
└─────────────────────┘
      ↓
User continues conversation
```

### 5. Error Handling Flow

```
Any Operation
      ↓
┌─────────────────────┐
│  Try Operation      │
└─────────────────────┘
      ↓ ERROR OCCURS
┌─────────────────────┐
│  ErrorBoundary      │ ← Catches React errors
│  (Component)        │
└─────────────────────┘
      ↓
┌─────────────────────┐
│  errorHandler.ts    │ ← Log error details
│  (logError)         │
└─────────────────────┘
      ↓
┌─────────────────────┐
│  errorHandler.ts    │ ← Convert to friendly
│  (getErrorMessage)  │   message
└─────────────────────┘
      ↓
┌─────────────────────┐
│  Display Error UI   │ ← Show user message
│                     │   with retry button
└─────────────────────┘
      ↓
User can:
├─ Retry operation
├─ Go back
└─ Continue using app
```

---

## Component Hierarchy

```
<ErrorBoundary>                          ← Catches all errors
  ↓
  <GlobalProvider>                       ← Provides user state
    ↓
    <ThemeProvider>                      ← Manages theme
      ↓
      <Stack.Navigator>                  ← Navigation
        ↓
        ├─ <IndexScreen />               ← Landing page
        │
        ├─ <AuthNavigator>               ← Auth routes
        │   ├─ <SignInScreen />
        │   └─ <SignUpScreen />
        │
        ├─ <OnboardingScreen />          ← Setup
        │
        ├─ <TabNavigator>                ← Main app
        │   ├─ <HomeScreen>              ← Video feed
        │   │   └─ <VideoCard> (×N)     ← Each video
        │   │
        │   ├─ <RoadmapScreen>           ← Learning path
        │   │   └─ <StepCard> (×N)      ← Each step
        │   │
        │   ├─ <CoursesScreen>           ← Saved courses
        │   │   ├─ <CourseCard> (×N)
        │   │   └─ <PlaylistPlayer>     ← Full player
        │   │
        │   └─ <DiscussionScreen>        ← AI chat
        │       └─ <AIChatModal>         ← Chat UI
        │
        └─ <ProfileScreen />              ← User profile
```

---

## API Integration Points

```
Mobile App
    │
    ├─── Appwrite API (Backend)
    │       │
    │       ├─ Authentication
    │       │   ├─ POST /account/sessions (login)
    │       │   ├─ POST /account (create user)
    │       │   └─ DELETE /account/sessions (logout)
    │       │
    │       └─ Database
    │           ├─ GET /databases/{db}/collections/{col}/documents
    │           ├─ POST /databases/{db}/collections/{col}/documents
    │           ├─ PATCH /databases/{db}/collections/{col}/documents/{id}
    │           └─ DELETE /databases/{db}/collections/{col}/documents/{id}
    │
    ├─── YouTube Data API v3
    │       │
    │       ├─ GET /search
    │       │   (Search videos by query)
    │       │
    │       ├─ GET /playlists
    │       │   (Get playlist details)
    │       │
    │       └─ GET /playlistItems
    │           (Get videos in playlist)
    │
    ├─── Google Gemini AI API
    │       │
    │       └─ POST /generateContent
    │           (Generate learning roadmap)
    │
    └─── Perplexity AI API
            │
            └─ POST /chat/completions
                (Chat responses)
```

---

## Database Schema

```
Appwrite Database
    │
    ├─── users (Built-in)
    │       ├─ id (auto)
    │       ├─ email
    │       ├─ name
    │       └─ password (hashed)
    │
    ├─── categories
    │       ├─ $id (auto)
    │       ├─ name (string)
    │       ├─ originalId (string)
    │       └─ description (string)
    │
    ├─── user_preferences
    │       ├─ $id (auto)
    │       ├─ userId (relation → users)
    │       ├─ categoryId (relation → categories)
    │       ├─ subcategoryId (string)
    │       └─ nicheId (string)
    │
    ├─── saved_videos
    │       ├─ $id (auto)
    │       ├─ userId (relation → users)
    │       ├─ title (string)
    │       ├─ url (string)
    │       ├─ thumbnail (string)
    │       └─ createdAt (datetime)
    │
    └─── roadmaps
            ├─ $id (auto)
            ├─ userId (relation → users)
            ├─ steps (json array)
            ├─ progress (json)
            └─ updatedAt (datetime)
```

---

## Build Process Flow

```
Source Code (TypeScript/JSX)
      ↓
┌─────────────────────┐
│  Pre-Build Check    │ ← Validate config
│  (Script)           │   Check .env
└─────────────────────┘   Verify images
      ↓
┌─────────────────────┐
│  Choose Build Type  │
└─────────────────────┘
      ↓
      ├─────────────────────────┬─────────────────────┐
      ↓                         ↓                     ↓
┌─────────────┐     ┌─────────────────┐   ┌──────────────┐
│  EAS Build  │     │  Local Build    │   │  Development │
└─────────────┘     └─────────────────┘   └──────────────┘
      ↓                         ↓                     ↓
Upload to Expo        Generate native      Run with Expo Go
      ↓               Android project
Cloud compilation           ↓
      ↓               Gradle build
Download APK                ↓
      ↓               Local APK
      └──────────┬────────┘
                 ↓
         ┌──────────────┐
         │  APK File    │ ← Installable app
         │  (Android)   │
         └──────────────┘
                 ↓
         Install on device
```

---

## File Organization Logic

```
project/
│
├─── /app                    ← SCREENS (What user sees)
│    File-based routing: folder = route
│
├─── /components             ← REUSABLE UI (Building blocks)
│    Used across multiple screens
│
├─── /context                ← GLOBAL STATE (Shared data)
│    Accessible everywhere
│
├─── /lib                    ← EXTERNAL SERVICES (APIs)
│    Communication with outside world
│
├─── /utils                  ← HELPER FUNCTIONS (Tools)
│    Common utilities
│
├─── /constants              ← FIXED VALUES (Settings)
│    Don't change at runtime
│
├─── /data                   ← STATIC DATA (Predefined)
│    Lists, categories, etc.
│
├─── /hooks                  ← CUSTOM LOGIC (Reusable)
│    React hooks for common tasks
│
├─── /assets                 ← MEDIA FILES (Images, fonts)
│    Non-code files
│
└─── /scripts                ← AUTOMATION (Build, setup)
     Run from command line
```

---

## Request-Response Pattern

### Example: Loading Home Feed

```
┌─────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  User   │────→│   App    │────→│ Backend  │────→│ YouTube  │
└─────────┘     └──────────┘     └──────────┘     └──────────┘
     │               │                 │                 │
     │ Opens home    │                 │                 │
     │───────────────→                 │                 │
     │               │ Get preferences │                 │
     │               │─────────────────→                 │
     │               │                 │                 │
     │               │ ["Python"]      │                 │
     │               │←─────────────────                 │
     │               │                 │                 │
     │               │ Search "Python" │                 │
     │               │─────────────────────────────────→ │
     │               │                 │                 │
     │               │           [Video list]            │
     │               │←───────────────────────────────── │
     │               │                 │                 │
     │  Show videos  │                 │                 │
     │←───────────────                 │                 │
     │               │                 │                 │
```

---

This architecture ensures:

- ✅ **Separation of concerns** (UI, logic, data separate)
- ✅ **Scalability** (Easy to add features)
- ✅ **Maintainability** (Clear structure)
- ✅ **Testability** (Each layer can be tested independently)
- ✅ **Reusability** (Components used multiple times)
